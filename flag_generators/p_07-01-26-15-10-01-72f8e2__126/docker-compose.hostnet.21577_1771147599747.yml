services:
  generator:
    platform: linux/amd64
    build:
      context: .
      network: host
    volumes:
    - ${INPUTS_DIR}:/inputs:ro
    - ${OUTPUTS_DIR}:/outputs
    environment:
    - OUT_DIR=/outputs
    - CONFIG_PATH=/inputs/config.json
    network_mode: host
