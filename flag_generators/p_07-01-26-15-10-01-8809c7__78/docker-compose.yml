services:
  generator:
    build: .
    volumes:
      - ${INPUTS_DIR}:/inputs:ro
      - ${OUTPUTS_DIR}:/outputs
    environment:
      - OUT_DIR=/outputs
      - CONFIG_PATH=/inputs/config.json
      - ARTIFACT_VARIANT_ID=${ARTIFACT_VARIANT_ID}
