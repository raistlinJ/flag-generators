# MD5 Password Hash Generator

This generator creates an MD5 hash of a common 5-letter word for reverse lookup challenges.

## Usage

Run with a seed to generate the hash.

## Outputs

- `Flag(flag_id)`: The MD5 hash of the password
- `Credential(user,password)`: The original password used
- `File(path)`: A file containing the hash for reference
