services:
  generator:
    build: .
    volumes:
      - ${INPUTS_DIR:-./inputs}:/inputs:ro
      - ${OUTPUTS_DIR:-./outputs}:/outputs
    environment:
      - CONFIG_PATH=/inputs/config.json
      - OUT_DIR=/outputs
      - SERVICE_VARIANT_ID=smb_it_scripts_share
