services:
  generator:
    build: .
    volumes:
      - ${INPUTS_DIR:-./inputs}:/inputs:ro
      - ${OUTPUTS_DIR:-./outputs}:/outputs
    environment:
      - CONFIG_PATH=/inputs/config.json
      - OUT_DIR=/outputs
      - WEB_VARIANT_ID=${WEB_VARIANT_ID:-http_support_ticket_portal}